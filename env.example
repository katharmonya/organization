# Rename to .env for local testing. On Render, add these as environment variables instead.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require
# Your GitHub Pages origin (no path). Separate several with commas.
ALLOWED_ORIGIN=https://YOUR-USERNAME.github.io
PORT=3000
